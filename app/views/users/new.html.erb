<div class="hero">
  <p class="large brandon" style="color: #DE0540">Skillout is live</p>
  <p class="small brandon" style="color: #601D2F">Discover the best dance teachers near you<br>...and start dancing.</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>